# deps
node_modules/
# expo / metro
.expo/
dist/
web-build/
.metro-health-check*
# native (CNG — se regeneran con `npx expo prebuild`)
/android
/ios
# logs / os
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
.DS_Store
# secretos / firmas — NUNCA versionar
.env
.env*.local
*.keystore
*.jks
*.p12
*.mobileprovision
google-services.json
GoogleService-Info.plist
.claude/
